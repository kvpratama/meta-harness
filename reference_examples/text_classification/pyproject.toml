[project]
name = "text-classification"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "datasets",
    "langsmith>=0.9.7",
    "litellm[google]>=1.89.3",
    "openai-harmony",
    "tenacity>=8",
    "tqdm",
]

[tool.uv]
package = false

[tool.ruff]
exclude = ["agents"]
